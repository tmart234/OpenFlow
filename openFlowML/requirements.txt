# Core dependencies for the ML training pipeline (train.py + combine_data.py).
# Installed by .github/workflows/ml_training.yml.
#
# NOTE: kept intentionally minimal. tensorflow 2.13 pins typing-extensions<4.6,
# which conflicts with earthaccess (>=4.10). The extra data-collection modules
# that need earthaccess live in requirements-data.txt and are installed by a
# separate CI job so the two never share an environment.
requests>=2.31.0
pandas>=2.1.1
scikit-learn>=1.3.2
numpy>=1.23,<1.25
scipy>=1.5.0
tensorflow==2.13
