# Dependencies for the standalone data-collection modules under openFlowML/data/
# (soil moisture, VegDRI, watershed/polygon tools).
#
# These are NOT needed to train the model. They are kept separate from
# requirements.txt because earthaccess requires typing-extensions>=4.10, which
# conflicts with the tensorflow 2.13 pin in requirements.txt. Install this file
# in an environment WITHOUT tensorflow.
geopandas>=1.0.1
shapely>=2.0.0
rasterio>=1.3.0
contextily>=1.4.0
matplotlib>=3.7.0
pyproj>=3.5.0
earthaccess>=0.10.0
h5py>=3.9.0
python-dotenv>=1.0.0
xarray>=2023.1.0
dask>=2023.1.0
fsspec>=2023.1.0
